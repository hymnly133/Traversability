[project]
name = "mujoco-traversability-demo"
version = "0.1.0"
description = "A MuJoCo demo for quantifying path traversability with geometric clearance metrics."
requires-python = ">=3.10,<3.13"
dependencies = [
  "matplotlib>=3.10",
  "mujoco>=3.9",
  "numpy>=2.2",
  "rich>=15.0",
]

[project.scripts]
traversability-demo = "traversability.evaluate:main"
multilevel-terrain-demo = "traversability.multilevel_demo:main"
verify-multilevel-reproduction = "traversability.verify_reproduction:main"
realtime-terrain-demo = "traversability.realtime_demo:main"
verify-implicit-map = "traversability.verify_implicit_map:main"
planner-ablation-demo = "traversability.ablation_demo:main"
tracking-demo = "traversability.tracking_demo:main"
pointcloud-demo = "traversability.pointcloud_demo:main"
benchmark-suite = "traversability.benchmark_suite:main"
paper-pipeline-demo = "traversability.paper_pipeline_demo:main"
paper-receding-demo = "traversability.paper_receding_demo:main"
paper-scenario-suite = "traversability.paper_scenario_suite:main"
verify-pointcloud-io = "traversability.verify_pointcloud_io:main"
verify-ndt-map = "traversability.verify_ndt_map:main"
verify-ndt-planner = "traversability.verify_ndt_planner:main"
verify-tracked-stability = "traversability.verify_tracked_stability:main"
verify-wheeled-stability = "traversability.verify_wheeled_stability:main"
verify-hybrid-local-planner = "traversability.verify_hybrid_local_planner:main"
verify-paper-pipeline = "traversability.verify_paper_pipeline:main"
verify-paper-receding = "traversability.verify_paper_receding:main"
verify-paper-scenarios = "traversability.verify_paper_scenarios:main"
verify-paper-mainline = "traversability.verify_paper_mainline:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/traversability"]
